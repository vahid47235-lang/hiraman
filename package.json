{
  "name": "hiraban",
  "private": true,
  "version": "1.0.0",
  "description": "HIRABAN — Luxury Nature, Wellness & Adventure Resort",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:admin": "pnpm --filter admin dev",
    "build": "pnpm --filter web build && pnpm --filter admin build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
